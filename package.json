{
  "name": "ninja-websocket-server",
  "version": "1.0.0",
  "description": "Ninja Master-Slave WebSocket Server for synchronized strikes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
